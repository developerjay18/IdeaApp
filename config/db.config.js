module.exports = {
  DB_NAME: 'idea_app_db',
  DB_URL: 'mongodb://127.0.0.1/idea_app_db',
};


// use  'localhost' firstly but if it not happens then use '127.0.0.1' u have another optoin too that is '0.0.0.0'